// MIDDLEWARE VAQTINCHA O'CHIRILGAN - Production uchun kerak bo'lishi mumkin
// Next.js 15+ da "middleware" eskirgan, "proxy" ishlatish tavsiya etiladi
// Hozircha localhost test uchun bu kerak emas

// Agar production da ngrok yoki boshqa proxy ishlatilsa, 
// next.config.mjs da rewrites/headers orqali sozlash mumkin

export {}

/*
// ESKI MIDDLEWARE KODI - Kerak bo'lganda qayta yoqish mumkin:

import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export function middleware(request: NextRequest) {
  // Clone the response
  const response = NextResponse.next()
  
  // Add ngrok-skip-browser-warning header to skip ngrok warning page
  response.headers.set('ngrok-skip-browser-warning', 'true')
  
  return response
}

// Apply to all routes
export const config = {
  matcher: '/:path*',
}
*/
